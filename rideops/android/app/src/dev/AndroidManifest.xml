<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- SOLO flavor dev: la API local corre en http://10.0.2.2:4000 (sin TLS).
         stg/prod hablan https y NO heredan esta excepción — Android bloquea
         cleartext por default desde API 28 y así debe quedarse allá. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
